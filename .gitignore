node_modules/
.DS_Store
*.log
.env
.env.local
# Never commit the Mekra app's .env even if linked
/Users/mekdesyared/Mekra-Catholic-Bible/.env

# Obsidian workspace state — personal, not content
.obsidian/workspace*
.obsidian/cache

# Source PDFs (~96MB) — too large for git, rarely change.
# Use `git add -f` to override if you want them tracked.
raw/sources/

# Compendium digital source files (.pages binaries, ~69MB+). The derived
# extraction at raw/catechism-digital/ is tracked instead.
raw/books/

# Intermediate docx — regenerable from docs/user-guide.md via pandoc.
docs/*.docx

# Defensive: an Obsidian path bug once created Users/mekdesyared/... here.
Users/
